# Docker Compose Environment Variables
# Copy this file to .env and configure for your environment

# ====================================
# Application Environment
# ====================================
NODE_ENV=development
# Options: development, production

# ====================================
# Service Ports
# ====================================
# Backend API port (default: 3001)
BACKEND_PORT=3001

# Frontend web port (default: 5173 for dev, 80 for production)
FRONTEND_PORT=5173

# ====================================
# Frontend Configuration
# ====================================
# URL where the frontend will be accessible
FRONTEND_URL=http://localhost:5173

# Backend API URL for frontend (used in production builds)
VITE_API_URL=http://localhost:3001

# ====================================
# LiveKit Server Configuration (Optional)
# ====================================
# These are optional default credentials for testing.
# The application primarily uses credentials from the UI settings,
# which are passed via HTTP headers on each request.
#
# If you want to set default credentials for development/testing:
# LIVEKIT_URL=ws://localhost:7880
# LIVEKIT_API_KEY=your-api-key
# LIVEKIT_API_SECRET=your-api-secret

# ====================================
# Docker Configuration
# ====================================
# Docker Compose project name (optional)
COMPOSE_PROJECT_NAME=livekit-dashboard
